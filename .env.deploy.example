# ============================================================
# Lupos — Deploy Environment Variables (Template)
# ============================================================
# Copy this file to .env.deploy and fill in the values.
# These are injected into the container at runtime via env_file.
# boot.js fetches remaining secrets from Vault at startup.
# ============================================================

VAULT_SERVICE_URL=
VAULT_SERVICE_TOKEN=
